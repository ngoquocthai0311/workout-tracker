import { ExerciseSummaryPipe } from './exercise-summary.pipe';

describe('ExerciseSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
